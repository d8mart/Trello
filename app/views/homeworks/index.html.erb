<p id="notice"><%= notice %></p>

<h1>Listing Homeworks</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>End date</th>
      <th>Author</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  
  <tbody>
    <div><h3>No Terminadas</h3></div>
    <% @homeworks.each do |homework| %>
      <tr>
        <td><%= homework.title %></td>
        <td><%= homework.end_date %></td>
        <td><%= User.find(id=homework.user_id).username  %></td>
        <td><%= link_to 'Show', homework %></td>
        <td><%= link_to 'Edit', edit_homework_path(homework) %></td>
        <td><%= link_to 'Destroy', homework, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
    
 
  </tbody>
    


<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>End date</th>
      <th>Author</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  
  <tbody>
   
   <div><h3>Completadas</h3></div> 
    <% @homeworks2.each do |homework| %>
      <tr>
        <td><%= homework.title %></td>
        <td><%= homework.end_date %></td>
        <td><%= User.find(id=homework.user_id).username  %></td>
        <td><%= link_to 'Show', homework %></td>
        <td><%= link_to 'Edit', edit_homework_path(homework) %></td>
        <td><%= link_to 'Destroy', homework, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
   
  </tbody>
  
</table>

<br>

<%= link_to 'New Homework', new_homework_path %>
